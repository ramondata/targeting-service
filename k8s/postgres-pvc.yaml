apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-targeting-service
  namespace: toggle-master
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
